# PostgreSQL Database Configuration
DB_HOST=localhost
DB_PORT=5432
DB_NAME=bortexel_archive
DB_USER=postgres
DB_PASSWORD=your_password_here
DATABASE_URL=postgresql://${DB_USER}:${DB_PASSWORD}@${DB_HOST}:${DB_PORT}/${DB_NAME}